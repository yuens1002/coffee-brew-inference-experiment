# Neon Postgres — use the direct connection URL (port 5432) for Railway (persistent process).
# The pooler URL (port 6432, hostname contains "-pooler") is for serverless/edge contexts.
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"
